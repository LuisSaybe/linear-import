import SwiftUI
import CSV
import Alamofire
import Apollo

struct TeamView: View {
    @EnvironmentObject var store: ApplicationStore<ApplicationState, ApplicationAction>

    var body: some View {
        VStack {
            Text("Team 1")
        }
    }
}


